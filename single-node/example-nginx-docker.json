{
	"id": "nginx",
	"container": {
		"type": "DOCKER",
		"docker":{
			"network":"BRIDGE",
			"image": "nginx",
			"portMappings": [
				{"containerPort": 80, "hostPort": 0, "protocol": "tcp"}
			]
		}
	},
	"cpus": 0.5,
	"mem": 128
}
